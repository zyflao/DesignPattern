package Command.TV;

public interface ICommand {
	/**
	 * 执行命令对应的操作
	 * 
	 * @param unknown_type
	 *            $name
	 * @param unknown_type
	 *            $args
	 */
	void execute();
}
